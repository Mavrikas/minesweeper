# Minesweeper
Classic minesweeper made with React

To run it:

1. Install [Node.js](https://nodejs.org/en/download/package-manager)
2. `npm install`
3. `npm run dev`

or
3. `npm run build`
4. `npm run preview`
